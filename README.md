# CsInvestmentsTracker

### Setup

```
$ sudo npm install -g @angular/cli
$ sudo npm install
$ cd backend
$ sudo npm install
$ node setup_db.mjs
```

create .env in /backend
add to .env:
STEAMMARKETAPIKEY=
STEAMAPIKEY=
SESSIONSECRET=